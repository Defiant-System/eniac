
{
	init() {
		// fast references
		this.root = window.find(".table-tools");
	},
	dispatch(event) {
		let APP = numbers,
			Self = APP.tools,
			el;
		switch (event.type) {
			// custom events
			case "some-event":
				break;
		}
	}
}

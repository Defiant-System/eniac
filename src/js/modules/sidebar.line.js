
// eniac.tools.line

{
	init() {
		
	},
	dispatch(event) {
		let APP = eniac,
			Self = APP.tools.line,
			// Line = Self.line,
			el;
		switch (event.type) {
			// native events
			case "blur-line":
				break;
		}
	}
}

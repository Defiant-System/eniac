
// eniac.sidebar.shape

{
	dispatch(event) {
		let APP = eniac,
			Self = APP.sidebar,
			Els = APP.sidebar.els,
			Shape = event.shape || APP.sidebar.shape.el,
			el;
		switch (event.type) {
			case "set-shape-style":
				break;
		}
	}
}


// eniac.sidebar.text

{
	dispatch(event) {
		let APP = eniac,
			Self = APP.sidebar.text,
			Text = event.text || APP.tools.text.text.el,
			el;
		switch (event.type) {
			case "custom-event-type":
				break;
		}
	}
}


const Cursor = {
	init() {
		
	},
	dispatch(event) {
		let APP = eniac,
			Self = Cursor,
			el;
		switch (event.type) {
			// system events
			case "window.keystroke":
				break;
			// custom events
			case "move-up":
				break;
		}
	}
};

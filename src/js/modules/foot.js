
// eniac.foot

{
	init() {
		// fast references
		this.els = {
			root: window.find("content > .foot"),
		};
	},
	dispatch(event) {
		let APP = eniac,
			Self = APP.foot,
			el;
		switch (event.type) {
			case "select-sheet":
				break;
		}
	}
}


// eniac.sidebar.image

{
	dispatch(event) {
		let APP = eniac,
			Self = APP.sidebar.image,
			Image = event.image || APP.tools.image.image.el,
			el;
		switch (event.type) {
			case "custom-event-type":
				break;
		}
	}
}

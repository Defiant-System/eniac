
// eniac.spawn.sidebar.chart

{
	init(parent) {
		
	},
	dispatch(event) {
		let APP = eniac,
			Self = APP.spawn.sidebar.chart,
			name,
			value,
			el;
		// console.log(event);
		switch (event.type) {
			case "populate-chart-values":
				// tab: Chart
				break;
			case "update-chart-name":
				break;
		}
	}
}

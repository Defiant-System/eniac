
// eniac.sidebar.sheet

{
	dispatch(event) {
		let APP = eniac,
			Self = APP.sidebar.sheet,
			Els = APP.sidebar.els,
			Shape = event.sheet || APP.tools.table.sheet.el,
			name,
			value,
			el;
		switch (event.type) {
			case "populate-sheet-values":
				break;
		}
	}
}
